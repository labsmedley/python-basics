[project]
name = "python_project"
version = "0.1.0"
description = "A simple Python package scaffold."
authors = ["Medley <medley@example.com>"]
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

[project.scripts]
python_project = "python_project.main:main"
